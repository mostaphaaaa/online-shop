@using OnlineShop.Domain.Enums.User
@model OnlineShop.Domain.ViewModels.User.AdminCreateUserViewModel

@{
    ViewData["Title"] = "افزودن کاربر جدید";
}

<link rel="stylesheet" href="~/css/admin/user-create.css" />

<h4 class="fw-bold py-3 mb-4 text-primary">
    @ViewData["Title"]
</h4>

@if (ViewBag.Error != null)
{
    var error = (AdminCreateUserResult)ViewBag.Error;

    switch (error)
    {
        case AdminCreateUserResult.EmailDuplicated:
            <div class="alert alert-danger">
                <h5 class="mb-0">
                    ایمیل وارد شده تکراری است و قبلاً کاربری با این ایمیل ثبت شده
                </h5>
            </div>
            break;

        case AdminCreateUserResult.InvalidImage:
            <div class="alert alert-danger">
                <h5 class="mb-0">
                    تصویر انتخاب شده معتبر نمی‌باشد
                </h5>
            </div>
            break;

        case AdminCreateUserResult.Error:
            <div class="alert alert-danger">
                <h5 class="mb-0">
                    اطلاعات را کامل کنید
                </h5>
            </div>
            break;

        case AdminCreateUserResult.MobileDuplicated:
            <div class="alert alert-danger">
                <h5 class="mb-0">
                    موبایل وارد شده تکراری است و قبلاً کاربری با این شماره موبایل ثبت شده
                </h5>
            </div>
            break;

        case AdminCreateUserResult.UserNameDuplicated:
            <div class="alert alert-danger">
                <h5 class="mb-0">
                    نام کاربری وارد شده تکراری است و قبلاً کاربری با این نام کاربری ثبت شده
                </h5>
            </div>
            break;
    }
}

<form asp-action="Create"
      enctype="multipart/form-data">

    <div class="row g-4">

        <!-- User Information -->
        <div class="col-md-7">
            <div class="bg-light p-4 rounded shadow-sm">

                <div asp-validation-summary="ModelOnly"
                     class="text-danger mb-3"></div>

                <div class="mb-3">
                    <label asp-for="FirstName"
                           class="form-label fw-bold"></label>

                    <input asp-for="FirstName"
                           class="form-control" />

                    <span asp-validation-for="FirstName"
                          class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="LastName"
                           class="form-label fw-bold"></label>

                    <input asp-for="LastName"
                           class="form-control" />

                    <span asp-validation-for="LastName"
                          class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="UserName"
                           class="form-label fw-bold"></label>

                    <input asp-for="UserName"
                           class="form-control" />

                    <span asp-validation-for="UserName"
                          class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Email"
                           class="form-label fw-bold"></label>

                    <input asp-for="Email"
                           class="form-control" />

                    <span asp-validation-for="Email"
                          class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Mobile"
                           class="form-label fw-bold"></label>

                    <input asp-for="Mobile"
                           class="form-control" />

                    <span asp-validation-for="Mobile"
                          class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="NationalCode"
                           class="form-label fw-bold"></label>

                    <input asp-for="NationalCode"
                           class="form-control" />

                    <span asp-validation-for="NationalCode"
                          class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Password"
                           class="form-label fw-bold"></label>

                    <input asp-for="Password"
                           class="form-control" />

                    <span asp-validation-for="Password"
                          class="text-danger"></span>
                </div>

                <div class="form-check mb-4">
                    <input class="form-check-input"
                           asp-for="IsActive" />

                    <label class="form-check-label"
                           asp-for="IsActive">
                    </label>
                </div>

                <div class="d-flex flex-wrap gap-2">
                    <button type="submit"
                            class="btn btn-primary">
                        افزودن کاربر
                    </button>

                    <a asp-action="Index"
                       class="btn btn-label-secondary">
                        بازگشت به لیست
                    </a>
                </div>

            </div>
        </div>

        <!-- Avatar + Roles -->
        <div class="col-md-5">

            <!-- Avatar -->
            <div class="bg-light p-4 rounded shadow-sm text-center mb-4">

                <h6 class="mb-3">
                    تصویر کاربر
                </h6>

                <div class="avatar-preview-wrapper">
                    <img src="/Avatars/NoPhoto.jpg"
                         id="imgAvatar"
                         class="avatar-preview"
                         alt="تصویر کاربر" />
                </div>

                <div class="mt-4 text-start">
                    <label asp-for="AvatarFile"
                           class="form-label fw-bold"></label>

                    <input asp-for="AvatarFile"
                           class="form-control"
                           onchange="ShowpImage(this);" />

                    <span asp-validation-for="AvatarFile"
                          class="text-danger"></span>
                </div>

            </div>

            <!-- Roles -->
            <div class="card shadow-sm">

                <h5 class="card-header">
                    نقش های کاربر
                </h5>

                @if (Model.Roles.Any())
                {
                    <div class="card-body">

                        <div class="row">

                            @foreach (var item in Model.Roles)
                            {
                                <div class="col-12 mb-2">

                                    <div class="form-check custom-option custom-option-basic">

                                        <label class="form-check-label custom-option-content"
                                               for="role_@item.Id">

                                            <input class="form-check-input"
                                                   name="UserSelectedRoles"
                                                   type="checkbox"
                                                   value="@item.Id"
                                                   id="role_@item.Id">

                                            <span class="custom-option-header">
                                                <span class="h6 mb-0">
                                                    @item.RoleName
                                                </span>
                                            </span>

                                        </label>

                                    </div>

                                </div>
                            }

                        </div>

                    </div>
                }
                else
                {
                    <div class="card-body">
                        <div class="alert alert-warning mb-0">
                            هیچ نقشی برای انتخاب وجود ندارد.
                        </div>
                    </div>
                }

            </div>

        </div>

    </div>

</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script src="~/js/admin/user-create.js"></script>
}